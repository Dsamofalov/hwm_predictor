# Canonical Ability Module Agent TZ

The canonical ability-module development contract is [`../ABILITY_AGENT_TZ.md`](../ABILITY_AGENT_TZ.md).

Ability is a logical module/ownership boundary and all current development is performed directly on `main`. Historical references to a dedicated `ability` branch are provenance only and do not define the current workflow.
