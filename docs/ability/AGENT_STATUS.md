# Ability Integration Status

Checkpoint: **2026-08-13**

Source lane: `ability`
Draft PR: `#1` (`ability` -> `main`)
Current ability docs HEAD after TZ refresh: **`e008eaefad2e03ee7fa7210e8eec06d3360128e9`**
Current validated functional ability SHA: **`a2c06ef10048486cc84239b045f3710e9f7db795`**
Authoritative hosted Windows run: **`31680364027` — PASS / conclusion `success`**
Check-suite: **`85941007397`**
Platform authority: **GitHub-hosted Windows 10/11 x64 + VS2022/MSVC only**

## Governance

- `docs/ABILITY_AGENT_TZ.md` is the canonical ability-agent contract and was actualized on 2026-08-13 for the current Hexing frontier.
- `docs/ability/ability_changelog.md` is the canonical ability development journal.
- `TESTS_CANON.md` governs atomic test execution and exact inventory/coverage requirements.
- Linux/WSL is diagnostic only and is not PASS/FAIL authority.
- Ability development does not merge itself into `main`; final integration belongs to main/integration agent.

## Closed Gribbomb boundary

- `gribbomb = partial_exact`, canonical risk `0.25`.
- Exact observed carrier self-removal for canonical carrier-sourced `Sbom` is represented.
- Predictive Earth/collateral magnitude remains unresolved and disabled.
- Do not reopen without new contradictory evidence.

## Closed Taunt boundary

- Validated functional SHA `7f143d9050d42a20300be3a54511cdae16682f0e`; hosted Windows run `31639884205` passed.
- Exact server tooltip, neighboring-friendly geometry and opportunity/control populations are pinned.
- `ra2`/`ral` collide across carrier-target and adjacent-ally controls; final DAMAGE destination is not used to reconstruct original intent.
- No carrier-specific redirect label and no numeric redirect probability exist in current corpus.
- Taunt remains `unresolved` and is closed for this pass.

## Closed Spider / `Sent` boundary

Validated sequence:

- `2596f59a065604dd5a525d19969712cebbd9c3eb` — corpus-wide `Sent` source controls; run `31644823929` PASS.
- `1744354e79713569f7598e424f890801db88c8d5` — exact wire corpus lock; run `31645840641` PASS.

Strict locked facts:

- 866 battle dirs;
- 182 `ent` battles;
- 806 `Sent` records;
- 315 zero-source, 491 nonzero-source;
- nonzero split `405 Entroots-without-Spider / 84 Spider+Entroots / 2 neither`;
- payload `source3 + target3 + 000000000`;
- second UID state-resolvable 806/806;
- current parser leaves `target_uid=None` in this evidence package.

Raw `Sent` is not Spider-specific and is not safely Entroots-exclusive because of the two Netshooter controls. No second Spider runtime effect or registry promotion is justified.

## Closed Child of the Light boundary

Validated evidence sequence:

- `58965925cfe09552e9e5a4e22ff3d2cae86cbd69` — exploratory school probe, run `31647277552` FAIL because literal raw school token `light` does not exist; retained as evidence.
- `87c661aadcbcfd1b9ffd750aef20c6e9418e4c89` — school inventory, run `31647544114` PASS.
- `7d63aad9ae992cd9b949da43a7ec42a82f627a7d` — decoded `bm_tooltips` metadata audit, run `31648327688` PASS.
- `c5a2acaded82d36e5c32b6af9833554a44c60ce2` — strict metadata closure, run `31679297822` PASS / check-suite `85938145984`.

Strict Child facts:

- 866 battle dirs;
- 108 carrier battles;
- 137 carriers;
- server spellbook: 651 actors / 2031 entries;
- school tokens: `neutral 1405`, `air 275`, `earth 144`, `cold 141`, `other 31`, `fire 18`, `nt 17`;
- no literal `light` token;
- `neutral/nt` mixes incompatible game-school semantics;
- decoded `bm_tooltips` in 108/108 carrier battles has only `abil_desc`, `abil_names`, `perk_hints`;
- exact same-battle mapping-key overlap with spellbook names = 0;
- `child_light_text_hits = 216`;
- `non_child_light_text_hits = 92`;
- `school_text_hits = 112`;
- decisive independent discriminator `non_child_school_light_hits = 0`.

Semantic ceiling: Child remains `unresolved`, closed at the missing per-spell Light-school discriminator. No runtime copy rule, hardcoded Light taxonomy, registry promotion, or inferred probability is allowed.

## Current lead — Hexing Attack

Weighted unfinished queue currently leads to **`hexingattack`** after actionability/blocker filtering.

### Validated baseline and current functional package

Functional SHA: **`a2c06ef10048486cc84239b045f3710e9f7db795`** — `test(ability): pin Hexing baseline and wire audit`.

Hosted Windows validation: **run `31680364027` PASS / conclusion `success`**, check-suite **`85941007397`** on the exact functional SHA.

Strict baseline now in `python/tests/test_hexingattack_evidence.py`:

- 866 battle dirs;
- 32 Hexing carrier battles;
- 88 carriers;
- carrier creatures: `333 = 41`, `269 = 27`, `268 = 20`;
- exact carrier ability sets: `caster,hexingattack,undead` (47) and `alive,caster,hexingattack,ragingblood,sacrificegoblin,swiftattack` (41);
- 32/32 identical Hexing tooltip descriptions;
- 115 carrier attacks, all `MELEE_ATTACK`;
- attack creatures: `333 = 94`, `269 = 16`, `268 = 5`;
- 12 parsed zero-cost same-actor/same-target status records: `sff = 5`, `crs = 4`, `slw = 3`;
- `crs` amount shapes `100 = 3`, `96 = 1`;
- `sff` amount shape `12 = 5`;
- `slw` amount shape `40 = 3`;
- raw Hexing attack windows contain 3 additional `Sray...` records left outside generic status grammar;
- tooltip lists Curse, Slow, Weakness and Disrupting Ray at expert level but supplies only “some probability”; there is no numeric probability/percentage/integer constant.

The same functional SHA added `python/hwm_solver/ability/hexingattack_wire_evidence.py`, a whole-corpus collision/layout auditor for candidate codes `crs/slw/sff/ray`.

### Current semantic boundary

The package is **evidence-only**. PASS does not imply:

- `ray == Disrupting Ray`;
- `sff == Weakness`;
- all candidate wires are Hexing-owned;
- proc probability is `12/115`, `15/115` or any other observed frequency;
- any runtime/registry promotion.

`ray` and `sff` remain raw candidate identities until independent normal-cast/server-spellbook/collision evidence proves them.

## Exact next work

1. Inspect/extract the full `HEXINGATTACK_WIRE_COLLISION_EVIDENCE` output from exact SHA `a2c06ef...` / run `31680364027`, or rerun the exact atomic Hexing node on hosted Windows if needed to recover the full report.
2. Convert exploratory lower bounds in the wire test into exact corpus assertions for every candidate code:
   - record counts;
   - payload fixed-width shapes;
   - source/target state presence;
   - decision actor/target agreement;
   - attack-bound counts;
   - Hexing vs non-Hexing attack-bound populations;
   - source ability-set collisions;
   - zero/positive field populations;
   - owner relationships;
   - server spellbook names and exact/compatible positive-cost controls.
3. Require an independent normal-cast/server-spellbook discriminator before assigning semantic spell names to `ray` or `sff`.
4. Do not infer probability from corpus frequency. Probability work starts only after event attribution is solved.
5. Do not change replay/runtime/registry until the wire identity and observed consequence reach a proof-supported boundary.
6. If independent identity remains confounded, close Hexing as a precise blocker and recompute `data/reports/ability-risk-current.json` for the next actionable unfinished ability.

## Current files to inspect first

- `docs/ABILITY_AGENT_TZ.md`
- `docs/ability/AGENT_STATUS.md`
- `docs/ability/ability_changelog.md`
- `TESTS_CANON.md`
- `python/tests/test_hexingattack_evidence.py`
- `python/hwm_solver/ability/hexingattack_evidence.py`
- `python/hwm_solver/ability/hexingattack_wire_evidence.py`
- `data/reports/ability-risk-current.json`

## Commit / CI rule

Every functional ability commit:

1. hosted Windows Ability CI on exact SHA;
2. wait for `completed + success` before claiming PASS;
3. separate bookkeeping commit updating `docs/ability/ability_changelog.md`, this file, and root `changelog.md`;
4. no speculative formulas/percentages and no weakened assertions for green CI.
